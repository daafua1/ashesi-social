import 'package:ashesi_social/utils/exports.dart';

// A file to store constants used throughout the app
class Constants {
  static Size size = Get.size;

  static const appBarColor = Color(0xff91413f);
}

const String appName = "Ashesi Social";
final user = AppUser().obs;
